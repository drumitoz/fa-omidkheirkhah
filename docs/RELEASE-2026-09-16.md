# نسخهٔ آمادهٔ بازبینی 2026-09-16

## تغییرات

۴۴ مقالهٔ فعال بازنویسی یا گسترش یافته‌اند. قالب خواندن، فهرست مطالب، منابع مستقیم، زمان مطالعه، مطالب مرتبط و چاپ یکدست شده است. صفحهٔ اصلی با حفظ هویت مشکی و طلایی، مسیر مطالعه و ارتباط روشن‌تر دارد. جست‌وجوی فارسی، فیلترها، برگشت مرورگر، منوی صفحه‌کلید و فرم‌های واتساپ اصلاح شده‌اند.

## بررسی پیش از انتشار

- validate-content.py: موفق؛ ۴۴ مقاله، ۴۵ URL نقشهٔ سایت، ساختار HTML، شناسه‌های یکتا، ارجاع‌ها، فایل‌های محلی و برچسب فرم‌ها.
- validate-navigation.mjs: موفق؛ مسیر مستقیم، جست‌وجوی فارسی/عربی، فیلتر، حالت بدون نتیجه، برگشت مرورگر، منو و Escape، کدگذاری صحیح متن واتساپ و اختیاری‌بودن ایمیل.
- site.js و article.js: بررسی نحو موفق.
- git diff --check: موفق.
- ۲۷ مقالهٔ غیرفعال بدون تغییر با مبنای 4d34126 تطبیق داده شدند.
- بررسی علمی، بازبینی منابع و بازنویسی آموزشی است؛ داوری انسانی مستقل یا مرور نظام‌مند انجام نشده است.
- پیش‌نمایش فایل محلی با سیاست مرورگر مسدود شد؛ بررسی بصری روی نسخهٔ عمومی پس از استقرار انجام می‌شود. این محدودیت دور زده نشد.
- سبک‌های واکنش‌گرا برای اندازه‌های کوچک بازبینی شده‌اند؛ آزمون روی دستگاه موبایل واقعی انجام نشده است.

## وضعیت انتشار

انتشار مستقیم روی main توسط بررسی خودکار مجوز رد شد: انتشار عمومیِ این دامنهٔ تغییرات به تأیید صریح نیاز دارد. main تغییر نکرده است. بررسی بصری نسخهٔ آنلاین و آزمون دستگاه موبایل انجام نشده؛ تا دریافت تأیید انتشار، این نسخه برای بازبینی نگهداری می‌شود.
